import styled from 'styled-components';

export const IndicatorWrapper = styled.div`
  .indicator {
    display: flex;
    overflow: hidden;
    width: 150px;
    .item {
      flex-shrink: 0;
    }
  }
`;
