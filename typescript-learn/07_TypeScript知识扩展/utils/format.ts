// 命名空间

export namespace A {
  export function sum(num1: number, num2: number): number {
    return num1 + num2;
  }
}

export namespace B {
  export function sum(num1: number, num2: number): number {
    return num1 * num2;
  }
}
